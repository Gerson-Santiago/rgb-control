# ==========================================
# Dependências do Sistema (Instalar via apt)
# ==========================================
# O projeto agora depende de bibliotecas nativas de C do GNOME (GTK4).
# A melhor prática é delegar o "core" ao apt, e usar o pip apenas para os utilitários de dev.
# Comando de sistema:
# sudo apt install python3-evdev libnotify-bin python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1

# ==========================================
# Dependências de Desenvolvimento (.venv)
# ==========================================
# Instale estes pacotes puramente em Python dentro do seu ambiente virtual
# Nota: PyGObject-stubs deve ser instalado separadamente usando --no-deps para ignorar o código C do PyGObject.
# Comando extra: pip install PyGObject-stubs --no-deps

evdev>=1.6.0
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.20.0
